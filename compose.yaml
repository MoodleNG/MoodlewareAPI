services:
  moodle-api:
    build: .
    container_name: MoodlewareAPI
    ports:
      - "8000:8000"
    restart: unless-stopped
    networks:
      - moodle-network

networks:
  moodle-network:
    driver: bridge
