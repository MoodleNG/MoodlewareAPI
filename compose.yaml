# This compose file is meant for development purposes.

services:
  moodle-api:
    container_name: MoodlewareAPI
    build: .
    ports:
      - "8000:8000"
    environment:
      - MOODLE_URL=http://moodle.bbbaden.ch
    restart: unless-stopped
